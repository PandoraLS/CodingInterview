#include <iostream>
using namespace std;
int main() {
    double a = 0.0, b = 0.0;
    while (cin >> a >> b) {
        if (a == b) {
            cout << "the number are equal \n";
        }
    }
    return 0;
}